<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubUserList" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/gold</item>
    </style>

    <style name="Theme.GithubUserList" parent="Base.Theme.GithubUserList" />

    <style name="Theme.GithubUserList.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
</resources>